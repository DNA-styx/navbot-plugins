# NavBot Admin Debug Tool

Plugin allow admins to debug bots in a dedicated server.

## Usage

Use the `sm_nbadmin_toggle_debug` console command to toggle debugging on/off.    
After turning debugging on, join the spectator team and spectate the bot you want to debug.    
The bot's current task are draw on the HUD and the bot's path is draw using tempents.    
